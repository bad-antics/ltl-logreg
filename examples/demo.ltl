// examples/demo.ltl — runnable demo for ltl_logreg

import std::io
use ltl_logreg as lib

fn main() {
    io::println("ltl_logreg demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}